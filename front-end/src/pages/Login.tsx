import LoginForm from "../components/Login/LoginForm";

type Props = {};
const Login = (props: Props) => {
  return (
    <div className="mt-[10%]">
      <LoginForm />
    </div>
  );
};
export default Login;
